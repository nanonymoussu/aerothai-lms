# ✈️ AEROTHAI LMS Auto-Progress Automation

Script สำหรับช่วยจัดการและอัปเดต Progress การเรียนบนระบบ AEROTHAI LMS ให้เป็น 100% โดยอัตโนมัติ เหมาะสำหรับผู้ที่ต้องการความสะดวกในการเรียนรู้หรือจัดการคอร์สเรียน

## ✨ ฟีเจอร์หลัก

- `Auto Login`: เข้าสู่ระบบให้อัตโนมัติ (พร้อมจัดการ CSRF Token)
- `Auto Detect`: ค้นหา Video ID และ Duration ของคลิปจากหน้าเว็บโดยอัตโนมัติ ไม่ต้องกรอกเอง
- `Form-Data Handling`: ส่งข้อมูลแบบ multipart/form-data ตรงตามมาตรฐานของระบบ LMS

## 📥 การติดตั้ง (Installation)

คุณสามารถดาวน์โหลดโปรเจคนี้มาใช้งานได้ผ่านคำสั่ง Git CLI:

1. Clone โปรเจคลงเครื่อง

    ```bash
    git clone https://github.com/nanonymoussu/aerothai-lms.git
    ```

2. เข้าไปยังโฟลเดอร์โปรเจค

    ```bash
    cd aerothai-lms
    ```

## 🛠️ โครงสร้างโปรเจกต์ (Project Structure)

```bash
aerothai-lms/
├── main.py      # Script หลักในการทำงาน
└── README.md    # คู่มือการใช้งาน
```

## 📋 สิ่งที่ต้องเตรียม (Prerequisites)

ก่อนใช้งาน ต้องมั่นใจว่าเครื่องของคุณติดตั้งสิ่งเหล่านี้แล้ว:

- Python 3.10+
- uv (Python Package Manager ที่รวดเร็ว) [แนะนำ]
  - ติดตั้ง uv: `pip install uv`

## สิ่งที่ต้องแก้ไขในโค้ด

```python3
// main.py

## --- CONFIGURATIONS ---
USERNAME = "your_username"  # <-- ใส่รหัสพนักงาน/Username
PASSWORD = "your_password"  # <-- ใส่รหัสผ่าน
BASE_URL = "https://lms.aerothai.co.th"
```

## 🚀 วิธีการใช้งาน (Usage)

รันคำสั่งบน Terminal

```bash
uv run main.py "<URL_ของหน้าวิดีโอ>"
หรือ
python main.py "<URL_ของหน้าวิดีโอ>"
```

ตัวอย่าง:

```bash
uv run main.py "https://lms.aerothai.co.th/Learning/Index/275?csm=3486&cb=1672&cs=915"
หรือ
python main.py "https://lms.aerothai.co.th/Learning/Index/275?csm=3486&cb=1672&cs=915"
```

## 📦 Dependencies

Script นี้ใช้ Library หลักดังนี้:

- `requests`: สำหรับจัดการ HTTP Request/Response
- `beautifulsoup4`: สำหรับดึงข้อมูล (Scraping) จากหน้า HTML
